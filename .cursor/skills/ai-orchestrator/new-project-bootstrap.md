# 新專案 Bootstrap

> **已合併至 [bootstrap.md](bootstrap.md)**。請以 `bootstrap.md` 為唯一流程。

快速指令：

```text
依 ai-orchestrator/bootstrap.md 初始化本 repo（Slack 頻道 #你的產品頻道）。
```

可選：`.workflow-specs/` 見 `templates/workflow-specs/`（manifest.json `optional` 區）。
