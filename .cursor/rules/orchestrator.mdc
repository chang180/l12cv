---
description: Cursor 作為本 repo 的 AI orchestrator 時的編排規則（由 ai-orchestrator skill bootstrap 產生，v4 local-first）
globs:
  - "**/*"
alwaysApply: true
---

# Orchestrator Rules（v4 local-first）

本專案 **Cursor IDE 為唯一主編排者**。Slack 產品頻道只做**進度看板**；`@Cursor` Cloud Agent **預設不用於派工**。

## Workflow v4

1. 任務寫入 `docs/tasks/T-*.md`；進度寫入 `docs/progress.md` → **git push**（SoT 在 GitHub）。
2. **本機**執行（Cursor / Claude Code / subagent）；讀任務檔，勿預設 Slack `[handoff]`。
3. 里程碑後可選 Slack **`[status]`** 至 `#10-proj-l12cv` — **勿 @ bot**。
4. 重大決策寫 `docs/decisions/*.md`，可鏡像 `[decision]` 至 `#42-knowledge-decisions`。
5. Slack execute 派工僅在 `progress.md` 標記 `exception: slack-delegate` 時；見 legacy `docs/agent-handoff/PROTOCOL-v3.md`（optional）。

## 執行優先順序

1. 讀 `docs/WORKFLOW-v4.md`、`docs/progress.md`、相關 `docs/tasks/*.md`。
2. `gh` 更新 issue / branch / PR / `docs/progress.md`。
3. 本機實作與複審。
4. 可選：Slack MCP 發 `[status]`（無 mention）。
5. `include_bots: true` 僅在復掃 roster 時使用。

## 禁止假設

- Workflow YAML 自動部署 Slack。
- MCP 邀請 bot、管理頻道區段。
- Slack `@executor` 會自動排隊執行。
- `handoff-complete` 可 @Cursor 當工作單（會誤開 PR）。

## 契約檔

- `docs/WORKFLOW-v4.md`
- `docs/progress.md`
- `docs/agent-contract.md`
- `docs/slack-handoff-template.md`
- `docs/agent-roster.md`

## 本專案

- **產品頻道**：`#10-proj-l12cv`（`C0B47UBS2HH`）
- **Repo**：`chang180/l12cv`
- **Skill**：`.cursor/skills/ai-orchestrator/`
